# Copier en .env et adapter. Ne jamais committer .env (cf. .gitignore).

# --- PostgreSQL ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=genie_info
DB_PORT=5432

# --- API ---
API_PORT=8000
CORS_ORIGINS=http://localhost:3000
JWT_SECRET=dev-secret-change-me

# --- Redis ---
REDIS_PORT=6379

# --- MinIO (stockage objet) ---
MINIO_USER=minio
MINIO_PASSWORD=changeme-too
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

# --- Bootstrap admin (utilisé par l'API pour poser le hash Argon2 réel) ---
ADMIN_EMAIL=depginfo@enset-ebolowa.cm
ADMIN_PASSWORD=ChangeMeOnFirstRun!
